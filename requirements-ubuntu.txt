# Install ROS 2 Jazzy/rclpy and colcon from official Ubuntu ROS repositories.
# Then install the project in a system-site-packages venv with: pip install -e '.[simulation]'
# Pin model-training framework against the selected Ubuntu/GPU driver separately.
