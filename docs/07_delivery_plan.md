# 分阶段实施与验收

当前状态：设计与文件骨架已创建；训练、MuJoCo、Agent、统计程序均未实现。以下入口为未来接口，不是当前能力。

| 阶段 | 入口设计 | 交付/验收 | 收缩方案 |
|---|---|---|---|
| M0仿真通信 | scripts/simulate.py | 资产许可、reset/step、状态单位、旧episode动作拒绝、确定性回放误差测试 | 单进程单物体 |
| M1数据基线 | scripts/collect.py | 完整回合、失败标签、数据划分；固定技能任务可重复完成 | reach/push两技能 |
| M2世界模型 | scripts/train.py | 独立控制模块的有限任务验证、学习曲线与未见初态；断点恢复检查 | 单技能固定奖励 |
| M3 Agent | scripts/run_agent.py | 白名单、结构化候选、检测闭环、超时与恢复 | 固定任务图+离线候选 |
| M4核心机制 | scripts/train_skill_outcome.py | 技能结果预测/校准、等预算B1/B2/B3消融 | 校准不足回退分数并缩小结论 |
| M5论文证据 | scripts/evaluate.py及export_results.py | 冻结多种子评估、原始表、图表、失败回放、版本溯源 | 缩减扰动轴而不删失败 |

## 预算估计方法

硬件工作假设：Ubuntu工作站、一张可用GPU、足够存储；型号/显存/驱动未知，不指定已验证版本。先做1个任务1个种子的1000步采样与100次更新计时，记录峰值显存、每步存储与训练吞吐。

单方法训练时间估计 = 环境交互数/有效采样吞吐 + 更新数/更新吞吐（分时方案）；异步方案以实测并发吞吐计算，不能直接相加后再声称并发收益。总GPU小时按方法×种子×配置计并加入预实验/失败重跑预算。数据量 = 控制步数×每步字节 + 视频；Agent成本 = 调用数×实际输入输出计费或本地推理时间。先完成容量测量再锁定步数与正式预算。

M0/M1可先CPU调试；不承诺CPU可完成研究训练。Ubuntu版本、Python、MuJoCo、PyTorch及CUDA组合在安装前查官方兼容性并锁定；本骨架不提供伪造依赖锁。

## 待确认问题（不阻止设计交付）

1. Ubuntu机器的CPU、GPU、显存和可用训练时长？
2. 是否指定机械臂，未来是否接实机？
3. 论文重点倾向高层长任务恢复还是视觉世界模型本身？
4. Agent可使用联网模型还是必须完全离线？
5. 可接受的实验期限和交互/存储预算？

## 边界

未完成外部新颖性检索、官方实现版本核验、资产许可核验、GPU或MuJoCo运行验证。基于本地论文的迁移设计不构成算法有效性证明；不承诺实时性、机器人安全或sim-to-real成功。
